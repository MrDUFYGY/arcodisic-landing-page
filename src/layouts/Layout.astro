---
import '~/assets/styles/tailwind.css';

import { I18N } from '~/utils/config';

import CommonMeta from '~/components/common/CommonMeta.astro';
import CustomStyles from '~/components/CustomStyles.astro';
import ApplyColorMode from '~/components/common/ApplyColorMode.astro';
import Metadata from '~/components/common/Metadata.astro';
import SiteVerification from '~/components/common/SiteVerification.astro';
import Analytics from '~/components/common/Analytics.astro';
import BasicScripts from '~/components/common/BasicScripts.astro';

// Uncomment line below to activate View Transitions
// import { ViewTransitions } from 'astro:transitions';

import type { MetaData as MetaDataType } from '~/types';

import Nav from '../components/ui/indexui/NavigationBar.astro';	
import Contacto from '../components/ui/indexui/ContactoBarra.astro';
import Footer from '../components/ui/indexui/FooterPrincipal.astro';




export interface Props {
  metadata?: MetaDataType;

}

const { metadata = {} } = Astro.props;

const { language, textDirection } = I18N;
---

<!doctype html>
<html lang={language} dir={textDirection} class="2xl:text-[20px]">
  <head>
    <link rel="icon" href="/favicons/favicon.ico">
    <CommonMeta />
    <CustomStyles />
    <ApplyColorMode />
    <Metadata {...metadata} />
    <SiteVerification />
    <Analytics />
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-2FTR17WKCF"></script>
<!-- <script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-2FTR17WKCF');
</script> -->
    <!-- <script async src="https://www.googletagmanager.com/gtag/js?id=G-2FTR17WKCF"></script>
  <script type="text/javascript" is:inline>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-2FTR17WKCF');
  </script> -->

    <!-- Uncomment line below to activate View Transitions -->
    <!-- <ViewTransitions fallback="swap" /> -->
  </head>

  <body class="bg-[#D9D9D9] dark:bg-[#222242]">
    
    <Contacto />
		<Nav />

    <slot />
    <Footer />
    <BasicScripts />

    <script>
      if ('startViewTransition' in document) {
        document.addEventListener('click', (e) => {
          const target = e.target;
          if (!target) return;
          const a = (target instanceof Element) ? target.closest('a[view-transition]') : null;
          if (a && a instanceof HTMLAnchorElement && a.href && a.origin === location.origin) {
            e.preventDefault();
            document.startViewTransition(() => {
              window.location.assign(a.href);
            });
          }
        });
      }
    </script>

    <!-- Netlify Identity: handle invite/recovery/login tokens anywhere on the site -->
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
    <script>
      (function () {
        // Detectar tokens de invitación/recuperación en la URL
        const hasIdentityToken = /(?:invite_token|confirmation_token|recovery_token)=/.test(location.hash);
        
        // Si hay un token, redirigir a la página de setup
        if (hasIdentityToken && !location.pathname.includes('/admin/setup-password')) {
          window.location.href = '/admin/setup-password' + location.hash;
          return;
        }

        // Solo en producción: configurar eventos de Netlify Identity
        const isLocalhost = location.hostname === 'localhost' || location.hostname === '127.0.0.1';
        if (!isLocalhost) {
          const identity = (window as any).netlifyIdentity;
          if (!identity) return;

          // Después de login exitoso, ir al CMS
          const gotoCMS = () => {
            try { location.hash = ''; } catch (e) { console.debug('Identity: could not clear URL hash', e); }
            window.location.href = '/admin/';
          };

          identity.on('login', gotoCMS);
          identity.on('signup', gotoCMS);
        }
      })();
    </script>

    <style is:global>
    
    </style>
  </body>
</html>
